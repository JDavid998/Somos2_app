<!DOCTYPE html>
<html>
  <head>
    <title>Somos - Sistema de Instalaciones</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Chartkick CDN con adaptador de fechas -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-adapter-date-fns/dist/chartjs-adapter-date-fns.bundle.min.js"></script>
    <%= javascript_include_tag "https://unpkg.com/chartkick@5.0.1/dist/chartkick.js" %>
  </head>

  <body class="bg-gray-50">
    <% if user_signed_in? && current_user.admin? %>
      <!-- Navigation Bar -->
      <nav class="bg-white shadow-sm border-b">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex items-center">
              <%= link_to "Dashboard", dashboard_path, class: "text-xl font-bold text-gray-900" %>
            </div>
            <div class="flex items-center space-x-4">
              <%= link_to "Instalaciones", installations_path, class: "text-gray-600 hover:text-gray-900" %>
              <%= link_to "Técnicos", technicians_path, class: "text-gray-600 hover:text-gray-900" %>
              <%= link_to "Horarios", schedules_path, class: "text-gray-600 hover:text-gray-900" %>
              <span class="text-sm text-gray-500">Hola, <%= current_user.name %></span>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 mb-4">
        <%= notice %>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 mb-4">
        <%= alert %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>