<!DOCTYPE html>
<html>
  <head>
    <title>Somos - Sistema de Instalaciones</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
   
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-adapter-date-fns/dist/chartjs-adapter-date-fns.bundle.min.js"></script>
    <%= javascript_include_tag "https://unpkg.com/chartkick@5.0.1/dist/chartkick.js" %>
  </head>

  <body class="bg-gray-900 text-gray-100 min-h-screen">
    <% if user_signed_in? && current_user.admin? %>
    
      <nav class="bg-gray-800 shadow-lg border-b border-emerald-400">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex items-center">
              <%= image_tag "logo_correo.png", alt: "Logo", class: "h-6" %>
            </div>
            <div class="flex items-center space-x-4">
              <%= link_to "Dashboard Admin", dashboard_path, class: "text-gray-300 hover:text-gray-100" %>
              <%= link_to "Instalaciones", installations_path, class: "text-gray-300 hover:text-gray-100" %>
              <%= link_to "Técnicos", technicians_path, class: "text-gray-300 hover:text-gray-100" %>
              <%= link_to "Administradores", admins_path, class: "text-gray-300 hover:text-gray-100" %>
              
           
              <div class="relative">
                <button id="logoutBtn" onclick="showConfirmation()" 
                    class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded text-sm transition-colors duration-200">
                  Cerrar Sesión
                </button>
             

                <div id="confirmLogout" class="hidden absolute right-0 top-full mt-2 bg-gray-800 border border-gray-600 rounded-lg shadow-lg p-4 z-50 min-w-max">
                  <p class="text-sm text-gray-300 mb-3">¿Estás seguro de cerrar sesión?</p>
                  <div class="flex space-x-2">
                    <%= form_with url: destroy_user_session_path, method: :delete, local: true, class: "inline" do |form| %>
                      <%= form.submit "Sí, cerrar", class: "bg-red-600 hover:bg-red-700 text-white px-3 py-1 rounded text-sm transition-colors" %>
                    <% end %>
                    <button onclick="hideConfirmation()" class="bg-gray-600 hover:bg-gray-700 text-gray-200 px-3 py-1 rounded text-sm transition-colors">
                      Cancelar
                    </button>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </nav>
    <% end %>

    
    <% if notice %>
      <div class="bg-green-800 border border-green-600 text-green-200 px-4 py-3 mb-4 mx-4">
        <%= notice %>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-800 border border-red-600 text-red-200 px-4 py-3 mb-4 mx-4">
        <%= alert %>
      </div>
    <% end %>

    <%= yield %>
    
   
    <script>
      function showConfirmation() {
        document.getElementById('confirmLogout').classList.remove('hidden');
        
        setTimeout(() => {
          document.addEventListener('click', hideOnClickOutside);
        }, 100);
      }
      
      function hideConfirmation() {
        document.getElementById('confirmLogout').classList.add('hidden');
        document.removeEventListener('click', hideOnClickOutside);
      }
      
      function hideOnClickOutside(event) {
        const confirmDiv = document.getElementById('confirmLogout');
        const logoutBtn = document.getElementById('logoutBtn');
        
        if (!confirmDiv.contains(event.target) && !logoutBtn.contains(event.target)) {
          hideConfirmation();
        }
      }
    </script>
  </body>
</html>